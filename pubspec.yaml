name: flutter_fimber_filelogger
description: A file logger tree for Flutter Fimber (which includes an auto-clean mechanism)
version: 1.1.1+1
author: Edouard Marquez <moi@edouard-marquez.me>
homepage: https://github.com/g123k/flutter_fimber_filelogger

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_fimber: '^0.3.2'

  path_provider: '^1.4.5'
  intl: '^0.16.0'
  path: '^1.6.4'
  synchronized: '^2.1.0+2'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: